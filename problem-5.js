// get the area of a rectangle using function
function getArea(height, width) {
    let area = height * width;
    return area;
}
let rectangle = getArea(15, 25);
console.log(rectangle, 'square unit');
